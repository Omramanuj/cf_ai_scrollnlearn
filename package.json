{
    "name": "cf_ai_scrollnlearn",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
      "install:all": "npm i --prefix frontend && npm i --prefix backend",
      
      "build:frontend": "npm install --prefix frontend && npm run build --prefix frontend",
      
      "deploy:backend": "npm install --prefix backend && npx wrangler deploy --config backend/wrangler.toml"
    },
    "devDependencies": {
      "concurrently": "^9.2.1"
    }
  }